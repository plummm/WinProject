#include "stdafx.h"
#include "Socket.h"
FILE *stream;

Socket::Socket()
{
}

void Socket::getRank(int score)
{
	//freopen_s(&stream, "rankb.txt", "w", stderr);
	//fprintf(stream, "Anonymous %d\n", score);
	//system("python client.py");

}

Socket::~Socket()
{
}
